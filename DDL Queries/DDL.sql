--
-- BE-IT DK2 A20
-- Gruppe 30
-- Benjamin Eibye, Christian K. Brodersen, Jonas Tvede og Ida Maria Ingerslev
--

--
-- To Use this document, you would need to remove the comment line "--" infront of the querys
--

-- Using CT DB
USE CT;

-- Gets a list of all Users
-- SELECT * FROM Users;

-- Gets a list of all Sessions
-- SELECT * FROM Sessions;

-- Gets a list of all Quizzess
-- SELECT * FROM Quizzes;

-- Selects all Quiz and questions. For use add a where statement: `Quiz id` = {Quiz ID}
-- SELECT * FROM Questions WHERE `Quiz id` = 1;

-- Gets a list of all answers. For use aff a where statement: `Session Id` = {Session Id} to get a list of all answers in that Session.
-- SELECT * FROM SessionAnswers WHERE `Session Id` = 1;
